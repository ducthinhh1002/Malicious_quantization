Đánh giá run `blind_20260922_023602_632293`, ngày 2026-09-22.

Run hoàn thành 24 nhánh, không có branch failure. Source hash trong manifest khớp code trước các chỉnh sửa tiếp theo của lượt này. Hash manifest/freeze khớp. Tính lại detector hai phía từ per-image matches của cả 26 điều kiện (24 nhánh + reference + smoothing) khớp báo cáo. Chưa chạy lại extractor độc lập vì ảnh/checkpoint đầy đủ nằm trên server.

**Kết quả: chưa có suppression detection.** Cả 99 ảnh được phát hiện ở reference vẫn bị phát hiện ở mọi nhánh. Reconstruction W4 còn làm ảnh reference vốn không được phát hiện trở thành được phát hiện (TPR 100%). Các nhánh khác TPR 99%.

| Điều kiện | BA | TPR | PSNR | SSIM |
|---|---:|---:|---:|---:|
| Reference | 98.58% | 99% | — | — |
| RTN W4 | 98.31% | 99% | 33.00 | 0.940 |
| Block reconstruction W4 | 98.38% | 99% | 34.92 | 0.957 |
| Natural residual W4 | 96.92% | 99% | 32.17 | 0.923 |
| Residual QAT W4 | 97.42% | 99% | 33.55 | 0.938 |
| GAN QAT W4 | 97.67% | 99% | 33.12 | 0.935 |
| Spectral W4 | 97.67% | 99% | 32.92 | 0.939 |
| Fine-tune FP32 | 98.19% | 99% | 39.26 | 0.990 |
| GAN fine-tune FP32 | 98.21% | 99% | 37.71 | 0.984 |

Block reconstruction cải thiện rõ chất lượng so với RTN, nên hữu ích làm baseline benign PTQ. Nó không được thiết kế để giảm ownership. Thêm GAN/spectral/scale trong cấu hình đang chạy chưa tạo được evasion; không diễn giải tên method nhiều hơn thành attack mạnh hơn.

Empirical FPR đo được 1/1.000 = 0.1%; Wilson CI95% khoảng 0.0177%–0.5643%. Đã kiểm tra negative hashes không giao với 52 ảnh natural train/search. Điều này không chứng minh FPR quần thể <=0.1%, nhưng giúp kiểm tra việc hiệu chỉnh detector. Ngưỡng vẫn cố định, không được chọn lại bằng pool này.

**Cấu hình run thực tế nhỏ hơn run trước đáng kể:**

- Natural TRAIN 32 ảnh, SEARCH 20 ảnh, batch 1.
- 100 update cho tất cả nhánh; fine-tune LR 1e-5; Adam.
- Preservation `legacy`, ảnh natural 512px.
- Run trước có 256 ảnh natural train, 200 update residual và 1.000 update QAT/fine-tune. Vì pool và ngân sách đều đổi, không kết luận các cải tiến thuật toán làm attack yếu hơn từ phép so hai run này.
- 16 nhánh optimized chọn checkpoint đúng bước cuối ngân sách. Đây là lý do thử ngân sách khác trên development, không phải bằng chứng chắc chắn cứ tăng step sẽ giảm TPR.

**Thay đổi code sau khi đọc run:**

1. `bash run_blind_quantization.sh` mặc định profile research: 4.000 ảnh natural train, 256 validation, batch 4, 1.000 update/nhánh; AdamW, warmup/cosine, LR fine-tune 5e-4. Natural resolution 256. Ghi toàn bộ args vào manifest. CLI override được; `WMQ_PROFILE=pilot` khôi phục cấu hình nhỏ.
2. Cùng low-pass preservation giữa các nhánh natural lượng tử hóa, cả khi tối ưu lẫn chọn checkpoint. Loại bớt confound khi so bật/tắt residual. GAN vẫn có thêm compute; không gọi các nhánh có cùng FLOPs.
3. Xuất/evaluate thêm fixed-final checkpoint nếu search chọn checkpoint sớm. Role riêng `endpoint_control`, cùng training với nhánh gốc, cả hai freeze trước test. Không dùng detector để chọn checkpoint thắng.
4. Ghi thêm số mẫu training đã dùng, số epoch tương đương và cờ chọn đúng biên ngân sách. Số update hợp lệ/skip vẫn được giữ.
5. Tiếp tục lưu failed branches, quality warnings và mọi kết quả; không dừng chỉ vì TPR/SSIM kém. Suite ba seed tiếp tục sau khi một run thất bại.

Đây là mở rộng kiểm chứng objective/data/optimization; chưa bảo đảm xóa watermark. GAN hiện dùng discriminator tự định nghĩa và LPIPS AlexNet, không phải bản tái lập Watson-VGG + HiDDeN. Spectral objective là natural log-amplitude reconstruction, không phải UnMarker. Vẫn chưa có surrogate ownership-transfer đã được đánh giá hoặc kernel INT4 thật.

Nếu cả FP32 control với cấu hình mới vẫn giữ TPR cao, cần ưu tiên xác minh latent/preprocessing và objective tự nhiên hoặc thử surrogate độc lập; không tiếp tục tăng DOF lượng tử vô hạn. Run này và run trước đã được dùng để phát triển, không còn là final holdout.
