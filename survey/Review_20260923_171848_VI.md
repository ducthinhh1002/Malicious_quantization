# Review run 20260923_171848

Nguồn: output_attack/run_20260923_171848/suite_results.csv, suite_status.json,
selection/search của từng nhánh. Run hoàn tất, không branch failure, 5179.19 giây
(86.32 phút). Một seed 3407, 100 ảnh TEST, 99 ảnh reference được phát hiện.

| Nhánh selected | Bit accuracy | TPR | SSIM | PSNR | Đồng thời thoát và đạt quality /99 |
|---|---:|---:|---:|---:|---:|
| Fixed W4 | 98.35% | 99% | .9396 | 33.01 | 0 |
| Natural rounding W4 | 79.85% | 80% | .8643 | 30.33 | 17 |
| Residual W4 | 70.98% | 41% | .8150 | 29.24 | 33 |
| Warm-QAT W4 | 70.75% | 40% | .8151 | 29.26 | 32 |
| Cycle warm-QAT W4 | 71.10% | 41% | .8149 | 29.26 | 32 |
| Teacher rounding W4 | 74.98% | 60% | .8390 | 29.77 | 31 |
| Fine-tune FP32 | 57.75% | 0% | .8119 | 28.82 | 56 |
| Fine-tune rồi W4 | 59.06% | 2% | .8063 | 28.36 | 51 |
| Joint fine-tune FP32 | 56.08% | 0% | .8102 | 28.78 | 57 |
| Joint fine-tune W4 | 55.65% | 0% | .8072 | 28.67 | 56 |

Chỉ dùng checkpoint được chọn trước owner evaluation trong bảng. Các endpoint
vẫn được giữ ở CSV, không chọn lại checkpoint tốt nhất bằng TEST.

## Diễn giải

Joint W4 đạt mục tiêu BA .5–.6 trên run này. So với fine-tune rồi RTN, BA giảm
3.42 điểm %, TPR giảm 2 điểm %, PSNR tăng .31 dB, LPIPS giảm .05257 xuống .04915.
Joint success tăng 51 lên 56/99. Đây là dấu hiệu thuận lợi, chưa là bằng chứng
ưu thế thống kê: CI95 joint success của hai phương pháp chồng lấp
(41.80–61.12% và 46.74–65.90%). TPR 0/100 không chứng minh tỷ lệ thực bằng 0;
cận trên Wilson hai phía 95% xấp xỉ 3.7% nếu coi các mẫu độc lập.

Nút thắt giờ là chất lượng: joint W4 chỉ 57/100 ảnh đạt đồng thời PSNR≥25 và
SSIM≥.8; joint success là 56/99 vì mẫu baseline không được phát hiện bị loại
khỏi mẫu số. Mean SSIM .8072 che khuất 43 ảnh không đạt quality từng ảnh.

Trong quantizer-only, residual vẫn là đối chứng chính: joint success 33/99,
warm/cycle 32/99, không có cải thiện đáng kể. Hai nhánh warm tốn thêm 1330 giây.
Code-change fraction so RTN vẫn xấp xỉ 3.253%; tỷ lệ offset≥1 ở cuối dưới
0.0002%. Chỉ số cuối là tỷ lệ độ lớn offset, không đo số mã thay đổi giữa các step.
Cycle SEARCH giảm .03435→.03220 mà TPR không tốt hơn: giảm cycle loss không
đồng nghĩa giảm watermark. Không nên chỉ tăng số bước vì kết quả này.

## Cải tiến triển khai

Thêm natural_joint_quality_finetune, cùng joint fine-tune nhưng có per-image
hinge penalty trên generated TRAIN cho cả FP32 và W4:

H = mean(relu(MSE / 10^(-p/10) - 1) + relu((s - SSIM)/(1-s))).

p=min_image_psnr=25, s=min_ssim=.8; trọng số .01, không đổi gate.
Ảnh tốt không bù trừ ảnh xấu vì lấy positive part trước trung bình. SEARCH tính
cùng penalty để xếp hạng; quality policy và double-tail owner evaluation giữ nguyên.
Giữ joint fine-tune cũ để đo tác dụng riêng. Nhánh mới cần thêm generated FP32/W4
forward/backward nên tốn bộ nhớ và thời gian; chưa benchmark GPU.
Có thể giữ watermark nhiều hơn: mục tiêu là tăng joint success, không hứa giữ TPR0.

Transfer bỏ hai warm-QAT để tiết kiệm; science vẫn giữ để tái lập/ablation.
Không xóa output cũ. Vẫn 2000 update/W4. Cả hai nhánh fine-tune nằm ngoài ràng buộc
quantizer-only dù artifact cuối là W4; không dùng kết quả đó để claim phá bằng PTQ thuần.

Đây là cải tiến sau khi đã xem TEST hiện tại, nên run này phải coi là development.
Kết luận cuối cần prompt/seed/key/checkpoint mới; không trình bày các lần lặp trên
cùng tập TEST như đánh giá độc lập. Chưa đủ dữ liệu claim novelty hay ưu thế bền vững.
